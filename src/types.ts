export type OptionsType = Record<string, number | boolean | string | any>; //TODO fix that any

export type CaseTypes = {
  kebabCase: string;
  snakeCase: string;
  upperSnakeCase: string;
  titleSnakeCase: string;
};
